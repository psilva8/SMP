Fix: Complete HTML replacement approach for area section

- Created area-section-replacement.js script to completely replace HTML
- Removed all dependencies on existing CSS and class structure
- Used pure inline styles without any class dependencies
- Added responsive behavior directly in the script
- Implemented a longer delay (500ms) to ensure DOM is fully loaded
- Created fresh HTML structure that doesn't inherit any problematic styles 