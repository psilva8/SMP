import type { Metadata } from "next";
import "./globals.css";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

export const metadata: Metadata = {
  title: "SMP Treatment | Professional Scalp Micropigmentation | Get Your Hair Back Today",
  description: "Professional scalp micropigmentation services for natural hair restoration results. Join hundreds who've restored their confidence with our expert SMP treatments in Los Angeles.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body>
        <Header />
        {children}
        <Footer />
      </body>
    </html>
  );
}
