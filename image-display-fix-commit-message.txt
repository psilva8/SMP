Fix: Add image display improvements and fallback mechanism

- Added inline styles to ensure proper image display
- Created image-load-fix.js script to handle image loading issues
- Implemented fallback mechanism if the main image fails to load
- Added cache-busting parameter to force image refresh
- Adjusted image styling (max-width, border-radius, etc.)
- Made image responsive with proper proportions 