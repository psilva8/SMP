Fix: Repair area dropdown menu loading in area pages

This fix resolves the issue with area dropdown menus not loading in area pages:

1. Created a script to populate the area dropdown menus with the same areas listed on the homepage
2. Added the script to all 168 area pages
3. Fixed relative URLs to work correctly from area pages
4. Made sure dropdown functionality works with the newly populated dropdown content

The areas menu now loads properly on all pages instead of showing "Loading areas..." indefinitely. 